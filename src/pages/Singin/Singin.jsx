import React from 'react';
import { useNavigate } from 'react-router-dom';

const Singin = () => {
  const navigate = useNavigate();
  return <div onClick={() => navigate('/home')}>signin0004444</div>;
};

export default Singin;
